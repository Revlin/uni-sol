% layout 'home';
% title 'Uni :Sol ::Vision';

<h1 id="text_title" style="display:none;">Vision
</h1>
<p>We value the perpetual expansion of creative techniques with the purpose of <em>re-defining the imagination</em>. We believe that the only solutions to emerging challenges are products of the imagination. In order to integrate these products into the personal, communal and global awareness, we require both pragmatic and visionary aids to development.
</p><br />
<%== 
$mediaDIV 
%>
<br />
<p>Uni:Sol is a <strong>creators collaborative</strong> (CC). This site provides resources to aid each author in the development of new interactive material. Use of this material by visitors generates revenue and a small portion of that revenue supports the management, provisioning, and further development of on-site resources. Both on-site resources and author-generated material are perpetually evolving.
</p><br />

<br /><br /><br />

<% 
{ 
	no strict 'vars';
  	if( defined $canvasApp ){ 
	# If URI for a Canvas app is provided, initialize "uni-sol">"layer1"
%>
  <script type="text/javascript" id="cvSrc" src="<%= $canvasApp %>"></script>
  <script type="text/javascript">
	function load() {
		var canvas=document.createElement("canvas");
		document.getElementById('uni-sol').children[0].replaceChild( canvas, document.getElementById("layer1") );
		canvas.addEventListener( "click", function(evt) {
			var git_link = document.getElementById("js-demos");
			git_link.target = "_blank";
			event = document.createEvent( "MouseEvent" );
			event.initEvent( evt.type, true, true );
			for( var e in evt ){
				event[e] = evt[e];
			}
			evt.preventDefault();
			git_link.dispatchEvent(event, true);
		}, false );
		canvasApp(canvas);
		if( typeof jQuery === "function" ) {
			jQuery('.cycle-slideshow').cycle('goto', 1);
			jQuery('.cycle-slideshow').cycle('pause');
		}
		
		if (typeof Debugger === "function") { 
			Debugger.on = false;
			return; 
		} else {
			window.Debugger = {
				log: function() {
					/* no debugger.js */
				}
			};
		}
	}
  </script>
  
<a id="js-demos" href="https://github.com/Revlin/js-demos#readme"></a>

<%
	} elsif( defined $svgApp ) { 
	# If URI for an SVG file is provided, initialize "uni-sol">"layer1"
%>
  <script type="text/javascript">
	function load() {
		document.getElementById("layer1").src = "<%= $svgApp %>";
		if( typeof jQuery === "function" ) {
			jQuery('.cycle-slideshow').cycle('goto', 1);
			jQuery('.cycle-slideshow').cycle('pause');
		}
		
		if (typeof Debugger === "function") { 
			Debugger.on = false;
			return; 
		} else {
			window.Debugger = {
				log: function() {
					/* no debugger.js */
				}
			};
		}
	}
  </script>
	
<%
	}
} 
%>
